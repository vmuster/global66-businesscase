fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
python-dotenv>=1.0
pyyaml>=6.0
httpx>=0.27

# LLM clients
instructor>=1.7
openai>=1.55
google-genai>=0.3
anthropic>=0.40
jsonref>=1.1            # required by instructor's GENAI_TOOLS mode (Gemini)

# Datos / batch
pandas>=2.2
openpyxl>=3.1
lingua-language-detector>=2.1

# Dashboard
streamlit>=1.40
plotly>=5.24

# Tests
pytest>=8.3
pytest-asyncio>=0.24

# One-Pager PDF builder (opcional pero recomendado)
# weasyprint genera el PDF pixel-perfect en Linux/Mac. En Windows requiere GTK
# (https://github.com/tschoonj/GTK-for-Windows-Runtime-Environment-Installer).
# El script scripts/build_onepager_pdf.py cae en xhtml2pdf como fallback puro
# Python si weasyprint no está disponible.
markdown>=3.10
weasyprint>=68 ; sys_platform != "win32"
xhtml2pdf>=0.2.17
